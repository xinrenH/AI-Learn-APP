# 智驭学途后续开发路线（Roadmap）

- 文档版本：v1.0
- 更新日期：2026-05-09
- 适用范围：`AI-Learn-APP` 当前仓库（Flutter + FastAPI + LangChain）

## 1. 总体目标

1. 8 周内完成可用 MVP（Android 首发）。
2. 打通“目标输入 -> AI生成计划 -> 执行打卡 -> 次日重排”闭环。
3. 在无数据库阶段先完成核心体验，随后平滑升级到持久化存储。

## 2. 阶段规划

## 阶段A：联调稳定（第1周）

目标：当前骨架可稳定演示、错误可感知。

交付：

1. 前端网络层统一错误提示（超时/500/业务错误）。
2. 后端统一响应格式与全局异常处理完善。
3. `plan/today` 无数据库模式稳定可用。
4. 基础联调文档（启动顺序、常见问题）完善。

验收标准：

1. 新用户 3 步内可看到计划页。
2. 接口异常时前端不崩溃，有明确提示。

## 阶段B：核心业务落库（第2-3周）

目标：从内存模式切换到 MySQL 持久化。

交付：

1. 引入 Alembic，完成首版迁移。
2. 核心表：用户、学习计划、学习任务、执行日志。
3. 新增接口：
   - `GET /api/v1/plan/today`
   - `POST /api/v1/plan/tasks/{id}/complete`
4. 前端计划页支持“完成打卡”与状态刷新。

验收标准：

1. 服务重启后用户计划不丢失。
2. 打卡后首页/计划页数据同步变化。

## 阶段C：AI能力增强（第4-5周）

目标：LangChain 从占位逻辑升级为可控生成。

交付：

1. 目标解析链：结构化抽取（Pydantic Schema）。
2. 计划生成链：Prompt 模板 + 结构化输出校验。
3. 失败兜底：模型超时/非法结构自动回退规则模板。
4. 生成日志：记录模型耗时、成功率、兜底率。

验收标准：

1. 结构化输出成功率 >= 95%。
2. 单次计划生成 P95 <= 8s（含兜底）。

## 阶段D：行为监督与重排（第6周）

目标：形成“执行反馈 -> 计划重排”闭环。

交付：

1. Android 端行为数据采集最小闭环（按权限能力降级）。
2. 后端分心统计聚合任务（Celery）。
3. 每日计划重排策略（未完成任务自动后移）。

验收标准：

1. 次日计划自动体现前一日执行结果。
2. 行为数据可在首页看到摘要统计。

## 阶段E：上线准备（第7-8周）

目标：达到灰度上线质量。

交付：

1. 可观测性：日志、关键指标、告警规则。
2. 性能与稳定性测试（基础压测 + 异常演练）。
3. 合规检查：权限说明、隐私策略、数据删除流程。
4. 发布清单：版本说明、回滚方案、运维手册。

验收标准：

1. 核心 API 可用性 >= 99.5%。
2. 发布流程可在 30 分钟内完成回滚。

## 3. 技术里程碑

1. M1：前后端稳定联调（当前周完成）。
2. M2：MySQL 持久化上线。
3. M3：LangChain 结构化生成上线。
4. M4：行为监督闭环上线。
5. M5：灰度发布。

## 4. 风险与应对

1. AI输出不稳定：强制结构化、失败兜底、可观测埋点。
2. Android采集兼容性：分机型白名单、降级策略。
3. 开发节奏被基础设施拖慢：坚持模块化单体，避免过早拆微服务。
4. 无数据库阶段数据易丢失：尽快完成阶段B并设迁移窗口。

## 5. 下一步（本周）

1. 落地 MySQL + Alembic 首版。
2. 实现任务打卡接口与前端按钮联动。
3. 接入 LangChain 第一版结构化目标解析。

