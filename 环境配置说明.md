# 智驭学途环境配置说明（Windows）

- 文档版本：v1.1
- 更新时间：2026-05-11
- 适用项目：`D:\AAALearn\AI+Learn-APP`

## 1. 目标

本文档用于一次性完成：

1. Flutter(Android) 前端环境配置
2. Python 后端环境配置
3. 本地联调启动与验证
4. `flutter doctor` 问题逐项排查

---

## 2. 基础环境要求

## 2.1 必装软件

1. Git for Windows
2. Python 3.11+（建议 3.11 或 3.12）
3. Android Studio（已安装）
4. Flutter SDK（Windows 版）

## 2.2 推荐目录

1. Flutter SDK：`D:\dev\flutter`
2. 项目目录：`D:\AAALearn\AI+Learn-APP`

---

## 3. Flutter 环境配置（Android）

## 3.1 安装 Flutter SDK

1. 下载 Flutter Windows SDK 压缩包。
2. 解压到：`D:\dev\flutter`
3. 确认存在：`D:\dev\flutter\bin\flutter.bat`

## 3.2 配置 PATH

### 临时生效（当前终端）

```powershell
$env:Path += ";D:\dev\flutter\bin"
flutter --version
dart --version
```

### 永久生效（用户变量）

```powershell
setx PATH "$env:PATH;D:\dev\flutter\bin"
```

说明：执行 `setx` 后需关闭并重新打开 PowerShell。

## 3.3 Android 工具链初始化

1. 打开 Android Studio，完成首次向导。
2. 在 SDK Manager 确保安装：
   - Android SDK Platform
   - Android SDK Build-Tools
   - Android SDK Platform-Tools
   - Android SDK Command-line Tools
   - Android Emulator
3. 接受许可证：

```powershell
flutter doctor --android-licenses
```

输入 `y` 直到完成。

## 3.4 诊断命令

```powershell
flutter doctor
flutter doctor -v
flutter devices
```

---

## 4. 后端环境配置（FastAPI）

在新终端执行：

```powershell
cd D:\AAALearn\AI+Learn-APP\backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 4.1 环境变量文件

```powershell
copy .env.example .env
```

当前是无数据库模式，`.env` 默认值可先不改。

### 4.2 启动后端（默认稳定模式）

```powershell
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 4.3 开发热重载模式（可选）

```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

说明：Windows 下 `--reload` 可能出现子进程残留，日常联调建议优先用稳定模式。

启动后验证：

1. `http://127.0.0.1:8000/healthz`
2. `http://127.0.0.1:8000/docs`

---

## 5. 前端环境配置（Flutter）

在新终端执行：

```powershell
cd D:\AAALearn\AI+Learn-APP\frontend
flutter pub get
flutter run
```

说明：

1. Android 模拟器下项目默认地址已配置为：`http://10.0.2.2:8000/api/v1`
2. 真机调试时需改为你电脑局域网 IP，例如：`http://192.168.1.10:8000/api/v1`
   - 文件：`frontend/lib/core/network/api_endpoints.dart`

---

## 6. 本地联调验证流程

1. 启动后端（保持运行）
2. 启动前端
3. 在 App 首页点“设定学习目标”
4. 输入学习目标，点“AI解析并生成计划”
5. 进入“今日计划”后点击“完成打卡”
6. 显示“已完成”即表示前后端联调成功

---

## 7. flutter doctor 常见问题与修复

## 7.1 报错：`flutter: 无法识别`

原因：Flutter 未安装或 PATH 未生效。

修复：

```powershell
$env:Path += ";D:\dev\flutter\bin"
flutter --version
```

若成功，再执行 `setx PATH ...` 做永久配置。

## 7.2 Android toolchain 未通过

修复：

1. 打开 Android Studio 安装缺失 SDK 组件
2. 执行：

```powershell
flutter doctor --android-licenses
```

## 7.3 未发现设备（No devices）

修复：

1. 启动 Android 模拟器（Device Manager）
2. 或连接真机并开启 USB 调试
3. 执行：

```powershell
flutter devices
```

## 7.4 PowerShell 执行策略阻止脚本（激活虚拟环境时报错）

修复（当前用户）：

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

然后重开终端再激活 `.venv`。

---

## 8. 后端进程卡住处理（Windows）

### 8.1 按命令关键字关闭 uvicorn 进程

```powershell
Get-CimInstance Win32_Process |
Where-Object { $_.CommandLine -like "*uvicorn app.main:app*" } |
ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

### 8.2 按端口关闭（8000）

```powershell
$pid = (Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue).OwningProcess
if ($pid) { Stop-Process -Id $pid -Force }
```

注意：`Stop-Process -Id` 后面直接写数字，不要加 `< >`。

---

## 9. 一键命令清单（可复制）

## 9.1 后端（稳定模式）

```powershell
cd D:\AAALearn\AI+Learn-APP\backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
copy .env.example .env
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

## 9.2 前端

```powershell
cd D:\AAALearn\AI+Learn-APP\frontend
flutter pub get
flutter run
```

## 9.3 诊断

```powershell
flutter doctor -v
flutter devices
```

---

## 10. 当前阶段说明

1. 当前后端为无数据库运行模式（计划数据存于内存）。
2. 后端重启后，计划与打卡状态会清空，属于预期行为。
3. 下一阶段可升级为 MySQL 持久化。
