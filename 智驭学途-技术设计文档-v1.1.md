# 智驭学途 —— 技术设计文档 v1.1（修订稿）

- 版本：v1.1
- 作者：项目组
- 日期：2026-05-09
- 状态：修订稿（可执行）

## 0. v1.1 变更摘要（相对 v1.0）

1. 收敛 MVP 范围：仅支持 Android（8.0+），iOS/小程序延期。
2. 架构降复杂：由“多微服务”调整为“模块化单体（Modular Monolith）”。
3. 中间件定版：消息中间件统一 RabbitMQ（不再并列 Kafka）。
4. AI 可控化：新增 JSON Schema 结构化输出、失败兜底策略、评测机制。
5. 指标可度量：补充核心指标计算口径与埋点定义。
6. 补齐非功能：新增安全合规、可观测性、备份容灾与 SLA 目标。

---

## 1. 系统概述

### 1.1 项目背景

智驭学途面向大学生，核心解决“想学但管不住自己”。

产品价值通过三个闭环实现：

1. 行为监督闭环：采集并分析手机使用行为，识别分心模式。
2. 资源匹配闭环：基于画像推荐适配资源。
3. 计划生成闭环：结合行为与资源动态生成学习计划。

### 1.2 MVP 范围（v1）

- 客户端：仅 Android App（Flutter）。
- 账号：手机号验证码登录（可扩展微信登录）。
- 核心功能：目标输入、学习计划生成、打卡执行、行为统计、资源推荐。
- 不包含：iOS、小程序、复杂社交功能、直播互动。

### 1.3 核心能力指标（v1）

| 指标 | 目标值 | 口径定义 |
|---|---:|---|
| 行为授权率 | >=70% | 安装后 24h 内完成行为权限授权用户数 / 新增用户数 |
| 行为有效上报率 | >=85% | 当日有至少 1 条有效行为事件用户数 / 已授权用户数 |
| 机型覆盖率 | >=90% | Top 机型中可正常采集行为数据机型数 / Top 机型总数 |
| 资源推荐满意度 | >=4.2/5 | 每周资源推荐反馈平均分 |
| 计划日完成率 | >=70% | 当日已完成任务数 / 当日计划任务总数（按任务） |
| 冷启动时长 | <=3 分钟 | 首次目标输入完成到首份计划可执行的耗时 |

### 1.4 约束与假设

- 平台：Android >= 8.0。
- 权限：用户主动授权 Usage Stats / 通知访问（按系统能力降级）。
- 知识库：首期 10 个考试品类，每品类 100+ 资源。
- 合规：仅采集最小必要行为数据，支持随时撤回授权并删除数据。

---

## 2. 技术架构总览

### 2.1 架构原则

- 先验证价值，再扩展规模。
- 业务内聚优先：v1 采用模块化单体，避免过早微服务化。
- 关键链路可观测、可回滚、可降级。

### 2.2 v1 总体架构（模块化单体）

```text
Android App (Flutter)
        |
   API Gateway(Nginx, 可选)
        |
Spring Boot 单体应用（按模块分层）
  - User Module
  - Behavior Module
  - Resource Module
  - Plan Module
  - AI Orchestration Module
        |
+-------------------------------+
| MySQL | Redis | RabbitMQ(统一) |
+-------------------------------+
        |
AI Provider(讯飞星火) + 检索服务(Chroma/混合检索)
```

说明：

- v1 默认单体部署，模块边界清晰（包结构、接口、事件）。
- 异步能力统一走 RabbitMQ；不引入 Kafka。
- AI 检索可先“标签+关键词”再逐步引入向量召回。

### 2.3 技术栈选型（v1）

| 层级 | 技术选型 | 版本建议 | 说明 |
|---|---|---|---|
| 前端 | Flutter | 3.x | Android 首发 |
| 后端 | Spring Boot | 3.x | 模块化单体 |
| 数据库 | MySQL | 8.0 | 核心业务数据 |
| 缓存 | Redis | 7.x | Session、限流、热点缓存 |
| 消息队列 | RabbitMQ | 3.x | 异步任务、通知事件 |
| AI接入 | 讯飞星火 API | v4.0 | 意图理解 + 计划生成 |
| 检索 | 关键词+标签（首期）/ Chroma（增强） | - | 渐进式引入向量检索 |
| 对象存储 | MinIO/OSS | - | 资源文件存储 |

---

## 3. 后端模块设计（单体内模块边界）

### 3.1 模块划分

1. `user`：注册登录、用户画像、权限状态。
2. `behavior`：行为事件接入、聚合统计、分心识别。
3. `resource`：资源入库、标签管理、推荐排序。
4. `plan`：计划生成、任务拆分、打卡与进度。
5. `ai`：Prompt 编排、模型调用、结构化校验、兜底。
6. `notification`：提醒与推送策略。

### 3.2 目录建议（Spring Boot）

```text
src/main/java/com/zhiyuxuetu/
  common/
  user/
  behavior/
  resource/
  plan/
  ai/
  notification/
```

### 3.3 关键异步任务（RabbitMQ）

- `behavior.event.ingest`：行为事件异步落库与聚合。
- `plan.daily.refresh`：每日计划重排。
- `notification.send`：学习提醒推送。

---

## 4. AI 服务设计（可控输出）

### 4.1 AI 调用链路

1. 目标输入解析（Intent Parser）。
2. 用户画像补全（历史行为 + 偏好）。
3. 资源候选召回（标签/关键词，后续可加向量）。
4. 计划生成（Plan Generator）。
5. Schema 校验 + 规则校验。
6. 成功返回；失败走兜底模板。

### 4.2 结构化输出 Schema（核心字段）

```json
{
  "goal": "证券从业资格证",
  "period_days": 30,
  "daily_hours": 3,
  "difficulty": "beginner",
  "tasks": [
    {
      "day": 1,
      "type": "video",
      "title": "证券市场基础",
      "duration_minutes": 90,
      "resource_id": "res_1001"
    }
  ],
  "review_strategy": "每3天一次小测"
}
```

### 4.3 失败兜底策略

- 模型超时：返回规则模板计划（按考试类型默认模板）。
- Schema 不通过：自动重试 1 次；仍失败走模板。
- 资源不足：给出“通用学习路径 + 待补充资源”提示。

### 4.4 Prompt 与版本管理

- Prompt 版本化：`prompt_id + version`。
- 每次生成记录：输入摘要、模型参数、输出结果、校验状态。
- 允许按用户分层灰度新 Prompt。

---

## 5. 数据存储设计

### 5.1 MySQL 核心表（示意）

- `user_profile`
- `user_permission`
- `behavior_event`
- `behavior_daily_stat`
- `learning_resource`
- `study_plan`
- `study_task`
- `plan_execution_log`
- `ai_generation_log`

### 5.2 Redis 使用场景

- 登录态与短期会话。
- 热点首页统计缓存。
- 接口限流计数（用户维度/IP 维度）。

### 5.3 数据生命周期

- 行为原始事件：保留 90 天。
- 聚合统计：保留 365 天。
- 用户申请删除：7 天内完成逻辑删除+物理清理。

---

## 6. 接口设计（示例）

### 6.1 用户与目标

- `POST /api/v1/auth/login`
- `POST /api/v1/onboarding/goal-parse`
- `POST /api/v1/onboarding/goal-confirm`

### 6.2 行为与统计

- `POST /api/v1/behavior/events`
- `GET /api/v1/behavior/daily-summary`

### 6.3 计划与执行

- `POST /api/v1/plan/generate`
- `GET /api/v1/plan/today`
- `POST /api/v1/plan/tasks/{taskId}/complete`

### 6.4 统一响应规范

```json
{
  "code": 0,
  "message": "ok",
  "trace_id": "xxxx",
  "data": {}
}
```

---

## 7. 安全合规与可观测性

### 7.1 安全与隐私

- 鉴权：JWT + Refresh Token。
- 数据最小化：仅上传学习相关行为字段，不采集隐私内容。
- 用户权利：可查看、导出、删除个人数据；可撤回授权。
- 传输与存储：HTTPS + 关键字段脱敏/加密。

### 7.2 可观测性

- 日志：结构化日志（含 `trace_id`）。
- 指标：接口 RT、错误率、AI 成功率、计划生成时长。
- 告警：5xx、队列堆积、AI 超时率异常。

### 7.3 SLA/SLO（v1）

- 核心 API 可用性：99.5%。
- 计划生成 P95：<=8s（不含大模型硬超时兜底）。
- 通知送达任务成功率：>=98%。

---

## 8. 部署方案（v1）

### 8.1 环境划分

- `dev`：开发联调
- `staging`：预发布验证
- `prod`：生产

### 8.2 部署形态

- 单体应用 2 副本（Nginx 负载均衡）。
- MySQL 主从或云托管高可用。
- Redis 单实例起步，后续哨兵。
- RabbitMQ 单节点起步，监控队列堆积。

### 8.3 备份与容灾

- MySQL：每日全量 + 每小时增量。
- 对象存储：跨可用区冗余。
- 关键配置：统一配置中心并版本管理。

---

## 9. 开发计划（8 周）

1. 第1-2周：需求冻结、数据模型、Android骨架、登录与引导。
2. 第3-4周：行为采集、计划生成主链路、基础推荐。
3. 第5-6周：AI结构化输出、兜底策略、埋点与监控。
4. 第7周：性能压测、灰度、缺陷收敛。
5. 第8周：上线准备、运维手册、复盘。

---

## 10. 版本演进建议

- v1.1：补充更多考试品类，优化计划重排策略。
- v1.2：引入向量检索混排，提高长尾推荐质量。
- v2.0：拆分微服务（当单体出现明确瓶颈时再拆）。

